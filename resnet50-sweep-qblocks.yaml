program: hydra_run.py

description: resnet50-sweep

method: bayes

command:
  - ${env}
  - ${interpreter}
  - ${program}
  - "--config-name"
  - "resnet50-sweep-qblocks"
  - ${args_no_hyphens}

metric:
  goal: maximize
  name: 'valid/acc'

parameters:
  optimizers:
    values: ['sgdnesterov', 'adamw']
    distribution: categorical
  schedulers:
    values: ['cosineannealingwarmup', 'sgdr-resnet50-sweep']
    distribution: categorical
  losses:
    values: ['crossentropy', 'labelsmoothing']
    distribution: categorical