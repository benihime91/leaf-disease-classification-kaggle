# @package augmentations
backend: custom

train:
  before_mix:
    img_size: ${image_dims}
    scale: null
    ratio: null
    hflip: 0.5
    vflip: 0.5
    interpolation: random
    auto_augment: null
    color_jitter: 0.1
    re_prob: 0.0
    mean: cassava
    std: cassava

  after_mix:
    img_size: ${image_dims}
    scale: null
    ratio: null
    hflip: 0.5
    vflip: 0.0
    interpolation: random
    auto_augment: rand-m11-n3-mstd0
    color_jitter: 0.1
    re_prob: 0.0
    mean: cassava
    std: cassava

valid:
  img_size: ${image_dims}
  crop_pct: null
  interpolation: bilinear
  mean: cassava
  std: cassava
