# @package augmentations
backend: custom

train:
  before_mix:
    img_size: ${image_dims}
    scale: null
    ratio: null
    hflip: 0.5
    vflip: 0.5
    interpolation: random
    auto_augment: null
    color_jitter: 0.2
    re_prob: 0.0
    mean: cassava
    std: cassava

  after_mix:
    img_size: ${image_dims}
    scale: null
    ratio: null
    hflip: 0.5
    vflip: 0.5
    interpolation: random
    auto_augment: rand-m10-n2
    color_jitter: 0.2
    re_prob: 0.5
    mean: cassava
    std: cassava

valid:
  img_size: ${image_dims}
  crop_pct: null
  interpolation: bilinear
  mean: cassava
  std: cassava
