# @package model
base_model:
  name: ${training.encoder}
  activation: null
  params:
    pretrained: true
    drop_path_rate: 0.25

head:
  name: CnnHeadV2
  params:
    n_out: 5
    dropout: 0.5
    act_layer: default
