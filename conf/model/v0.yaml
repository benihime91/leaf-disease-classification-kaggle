# @package model
base_model:
  name: ${training.encoder}
  activation: null
  concat_pool: false
  params:
    pretrained: true
    drop_path_rate: 0.25

head:
  name: CnnHeadV0
  params:
    n_out: 5
    drop: 0.3
    concat_pool: false
    act_layer: null
