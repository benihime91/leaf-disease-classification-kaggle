# @package model
base_model:
  name: ${training.encoder}
  activation: null
  params:
    pretrained: true
    drop_path_rate: 0.25

head:
  name: CnnHeadV0
  params:
    n_out: 5
    pool_type: "avg"
    use_conv: false
    act_layer: default
